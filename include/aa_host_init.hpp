#ifndef ASTRO_ACCELERATE_AA_HOST_INIT_HPP
#define ASTRO_ACCELERATE_AA_HOST_INIT_HPP

namespace astroaccelerate {
  /** \warning This function does not look like it has an implementation since there is no file called aa_host_init.cpp or similar. */
  void init(int argc, char *argv[], FILE **fp, int *tdms, float *dm_step, float *dm_low);

} // namespace astroaccelerate
  
#endif // ASTRO_ACCELERATE_AA_HOST_INIT_HPP

